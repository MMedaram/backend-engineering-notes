You are working on my Java notes.

Goal:
Create or update the notes so they are useful for a Java developer in real daily work. Keep one clean explanation per feature, avoid repetition across Java versions, and make the notes practical, correct, and complete.

What to do:

1. First inspect the existing notes.
2. If the notes are already correct and well-structured, keep them.
3. If the same explanation is repeated across multiple Java versions, merge it into one shared section.
4. Keep version-specific differences only where they actually matter.
5. Focus on how a Java developer would use the feature in real projects.
6. Include practical daily-use examples, not just theory.
7. Include important edge cases, limitations, gotchas, and best practices.
8. Show better approaches when a feature has multiple ways to use it.
9. Add notes about when to use it, when not to use it, and common mistakes.
10. Preserve correct content, but improve clarity, structure, and usefulness.

For each feature, include:

* feature name
* short explanation
* why a developer uses it
* syntax or example
* real-world usage
* edge cases and limitations
* best practices
* version timeline
* final status

Version handling:

* Mention only version changes that matter.
* If a version has no real change, say so briefly.
* If a feature was preview/incubator before becoming standard, show that progression clearly.
* Do not repeat the full explanation in every version note.

Preferred format:

Feature: <feature name>

Overview: <shared explanation>

Why developers use it: <practical reason>

Example: <code example>

Real-world usage: <daily-use scenario>

Edge cases / limitations:

* <edge case 1>
* <edge case 2>

Best approach: <preferred way to use it>

Version timeline:

* Java 13: preview
* Java 14: preview
* Java 15: standard

Notes: <only version-specific differences>

Rules:

* Avoid duplicate content across versions.
* Do not miss important edge cases.
* Do not skip daily-use patterns.
* Prefer the best practical approach over multiple weak alternatives.
* Keep the notes structured, concise, and developer-focused.
* If something is unclear, improve the structure while keeping the meaning unchanged.

Now review my notes and make the necessary changes.
